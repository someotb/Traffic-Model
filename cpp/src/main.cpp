#include "common.hpp"
#include "traffic_models.hpp"

int main(int argc, char *argv[])
{
    launch_options l_o = opts_parser(argc, argv);
    if (l_o.type_of_model == ModelType::poissonian)
    {

    }
    return 0;
}
